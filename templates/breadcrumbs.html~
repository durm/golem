<nav class="breadcrumbs">
    <ul>
    {% if rubric %}
        <li>
            <a href="{{ url_for('taxonomy_rubric') }}">Главная</a>
        </li>
    {% endif %}
    {% for step in rubric_path %}
        <li>
            {% if rubric.id == step.id %}
                <span>
            {% else %}
                <a href="{{ url_for('taxonomy_rubric', id=step.id) }}">
            {% endif %}
            {{ step.name }}
            {% if rubric.id == step.id %}
                </span>
            {% else %}
                </a>
            {% endif %}
        </li>
    {% endfor %}
    </ul>
</nav>
